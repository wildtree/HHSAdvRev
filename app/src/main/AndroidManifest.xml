<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />
    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:backupAgent=".ZDataBackupAgent"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HHSAdvRev">  <!-- ここで DayNight 対応のテーマを指定 -->
        <!-- android:theme="@style/Theme.AppCompat.NoActionBar"> -->
         <activity
             android:name=".SettingsActivity"
             android:theme="@style/Theme.HHSAdvRev.SettingScreen"
             android:exported="false"
             android:label="@string/title_activity_settings">
             <meta-data
                android:name="android.app.lib_name"
                android:value="" />
             <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.HHSAdvRev.NoActionBar"
            android:exported="true">
            <meta-data
                android:name="com.google.android.backup.api_key"
                android:value="AEdPqrEAAAAIrxNUp00Gla43XwICg6LwI3cRIzAdnB3M-zOyEg" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ZCreditRollActivity"
            android:theme="@style/Theme.HHSAdvRev.FullScreen"
            android:exported="false" />
    </application>

</manifest>